import { Grid } from "@mui/material";

function Combo({combo}) {

    return (
        <div>
            <h2>
                Combo
            </h2>
            <Grid container spacing={6} p={0}>
                {"test"}
            </Grid>
        </div>
    )
}

export default Combo;